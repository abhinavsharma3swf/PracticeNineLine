package com.example.demo.repository;

import com.example.demo.entity.NineLine;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NineLineRepo extends JpaRepository<NineLine, Long> {
}
